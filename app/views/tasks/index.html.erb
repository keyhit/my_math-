<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>
<ul>
  <li>
    <%= link_to 'Task 1', controller: 'tasks', action: 'task_1' %>
  </li>
</ul>

<ul>
  <li>
    <%= link_to 'new', tasks_new_path %>
  </li>
</ul>

<table>
  <tr>
    <th>index</th>
    <th>Condition</th>
    <th>Input</th>
    <th>Result</th>
    <th>Action</th>
  </tr>
  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.id %></td>
      <td><%= task.condition %></td>
      <td><%= task.input %></td>
      <td><%= task.result %></td>
      <td><%= link_to 'Destroy', task_path(task),
                    method: :delete,
                    data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
<%= @volume %>
<%= @square %>